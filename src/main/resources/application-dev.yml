spring:
  config:
    activate:
      on-profile: dev

  datasource:
    username: root
    password: emmmm000

springdoc:
  default-flat-param-object: true
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

log:
  path: D:\code\kotlin\Note\
  root:
    level: info
